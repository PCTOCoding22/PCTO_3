#include <stdio.h>

int main()
{
    int x=2;
    int y=4;
    int z=6;
    if (z-y==x)
    {
        printf("il humero è in progressione aritmetica");
    }
    else if (z-y!=x)
    {
        printf("il numero non è in progressione aritmetica");
    }
    return (0);
}