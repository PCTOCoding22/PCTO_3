#include <stdio.h>

int main()
{ int x=4;
  int y=8;

  if (x%y==0) {
  
      printf ("x è multiplo di y");
  }
  else{ 
      (x%y!=0);
      printf("x non è multiplo di y ");
  }

  return 0;
} 
